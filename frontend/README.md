# frontend/

React + TypeScript + PouchDB. Sincronización offline-first con CouchDB.

## Convenciones

- `PascalCase` para componentes (`EditorView.tsx`)
- `camelCase` para hooks (`usePouchSync.ts`) y utilidades
- Un componente por archivo
