# backend/app/core/

Configuración global, seguridad, logger y puertos (interfaces abstractas).

## Subdirectorios

- `ports/` — Protocols de Python que definen las interfaces que `services/` consume
